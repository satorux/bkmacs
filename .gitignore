__pycache__/
*.py[cod]

# The temporary file the atomic save writes next to the file it is saving,
# which only exists for the moment between writing and renaming.
.bkmacs-*.tmp

# Encrypted files, which are only ever here because somebody was testing
# against a real one.  Encrypted is not a reason to publish it.
*.ossl

.DS_Store
